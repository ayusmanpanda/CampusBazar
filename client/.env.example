# Optional — only needed when running the Spring Boot backend, where Socket.IO listens on a separate port.
# Leave unset (or remove) when running the Node backend, since it proxies /socket.io via Vite.
# VITE_SOCKET_URL=http://localhost:9092
